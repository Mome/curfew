# curfew — Project Instructions

`curfew` is a single bash script (`./curfew`) with no build step and no Python packaging — run it
directly (`./curfew ...`), don't look for `uv`/`pyproject.toml`/a venv here.

## Skills

Before starting any task, check whether it makes sense to use the `superpowers` skills
(e.g. brainstorming, systematic-debugging, test-driven-development) instead of proceeding
ad hoc.
